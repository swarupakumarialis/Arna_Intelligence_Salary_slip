/* ARNA Learning Intelligence Studio — default logo.
   Bundled as a static asset (not inlined as base64) so it's served as
   a normal same-origin image URL — smaller JS bundle, and works with
   html2canvas's PDF capture without any CORS handling. */
import arnaLogoPng from './Arna_logo.png';

export const ARNA_LOGO_DATA_URI = arnaLogoPng;
